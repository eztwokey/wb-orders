apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: wb-orders
spec:
  selector:
    app.kubernetes.io/name: order-service
  ports:
    - name: http
      port: 80
      targetPort: http
